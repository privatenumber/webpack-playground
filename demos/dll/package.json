{
	"name": "@webpack-playground/dll",
	"description": "Splitting bundles with DLL to improve build-time",
	"scripts": {
		"build": "rm -rf dist && webpack --config webpack.vendor.config && webpack --config webpack.app.config"
	}
}
